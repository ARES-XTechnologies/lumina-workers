runpod==1.7.7
# ONNX Runtime rather than torch: RestoreFormer++ ships as .onnx, and this
# keeps the image small and the cold start quick.
onnxruntime-gpu==1.19.2
opencv-python-headless==4.10.0.84
pillow==11.0.0
numpy==2.1.3
